[package]
name = "nf-ingest"
version = "0.1.0"
edition = "2021"

[dependencies]
chain-ingest = { path = "../../shared/chain-ingest" }

[dev-dependencies]
hashtable-pir = { path = "../hashtable-pir" }
tokio = { version = "1", features = ["full"] }
tonic = "0.12"
tokio-stream = "0.1"
